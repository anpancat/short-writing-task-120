import React from "react";
import ReactDOM from "react-dom/client";
import WritingTest from "./WritingTest"; // WritingTest.jsx 불러오기

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <WritingTest /> {/* WritingTest 컴포넌트 사용 */}
  </React.StrictMode>
);
